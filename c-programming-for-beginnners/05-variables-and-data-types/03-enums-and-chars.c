// NOTE: Enums

// A data types that allows you to create a variable,
// and specify the values that could be stored into that variable


int main() {

    enum lightsaberColor {blue, red, green, purple};

    return 0;
}
